load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes jobs.
  - grpc_server:
      host: "mycluster-usercode.mycluster.local"
      port: 4000
      location_name: "usercode-jobs"
